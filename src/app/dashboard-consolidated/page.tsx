import ConsolidatedDashboard from '../components/ConsolidatedDashboard';

export const metadata = {
  title: 'Consolidated Dashboard | Stanton Management',
  description: 'Priority-based action items across all projects',
};

export default function ConsolidatedDashboardPage() {
  return <ConsolidatedDashboard />;
}
